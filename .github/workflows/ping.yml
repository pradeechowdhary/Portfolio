name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Ping Render URL
        run: curl -s https://pponnam-q3g6.onrender.com || true
