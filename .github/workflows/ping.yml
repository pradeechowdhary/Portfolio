name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to ping Render app
        run: curl --silent https://pponnam-q3g6.onrender.com > /dev/null
