name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to ping Render app
        run: curl --silent https://pponnam-b9e6.onrender.com > /dev/null
